{
  "name": "pastel-barraca-controle",
  "version": "1.0.0",
  "description": "Sistema de controle operacional para barraca de pastel 24h",
  "main": "index.html",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy --prod",
    "watch": "npx tailwindcss -i ./src/css/tailwind-input.css -o ./src/css/style.css --watch",
    "firebase": "firebase emulators:start",
    "start": "http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pastelaria",
    "controle",
    "turnos",
    "caixa",
    "firebase"
  ],
  "author": "Pastel 24h",
  "license": "MIT",
  "dependencies": {
    "firebase": "^10.7.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "firebase-tools": "^12.0.1",
    "http-server": "^14.1.1",
    "postcss": "^8.5.3",
    "serve": "^14.2.0",
    "tailwindcss": "^4.1.7",
    "vercel": "^32.5.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
