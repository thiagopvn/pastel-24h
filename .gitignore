# — dependências —
node_modules/

# — builds / saídas —
dist/
.next/
.out/
.vite/

# — artefatos temporários —
*.log
*.tar.gz
*.local

# — arquivos de configuração específicos do Replit —
.replit
replit.nix
replit.*

# — variáveis de ambiente —
.env
.env.*
!.env.example      # (mantenha se usar um modelo sem dados sensíveis)

# — arquivos do sistema —
.DS_Store
