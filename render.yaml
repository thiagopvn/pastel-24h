services:
  - type: web
    name: pastelaria-24h
    runtime: node
    buildCommand: npm install && npm run build && npm run db:push
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: postgresql://postgres:Pastel24h%21%40%23%24@db.axmbupauzyommvlxabop.supabase.co:6543/postgres
      - key: SESSION_SECRET
        generateValue: true
    autoDeploy: true